apply plugin: 'java'

group = "org.sekka"
version = '0.0.1'
archivesBaseName = "SekkaTest"

sourceCompatibility = targetCompatibility = 1.8

dependencies {
    compile files('../build/libs/Sekka-1.12.2-2835-0.0.1.jar')
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}
